import React from "react";

const CheckComponent = props => {

  // const arr = [1, 2, 3, 4, 5, 6, 7, 8, 9, 0];
  // const arr = [
  //   { id: 0, name: "Lee", age: 1  },
  //   { id: 1, name: "Kim", age: 30  },
  //   { id: 2, name: "Park", age: 20  },
  //   { id: 3, name: "Yoon", age: 11  },
  //   { id: 4, name: "Yu", age: 12  },
  // ];
  // const result = arr.some(item => item === 0);
  // const result = arr.every(item => typeof item === "number");
  // const result = arr.map(item => { if (item > 5) return item; });
  // const result = arr.filter(item => item > 5);
  // 백엔드 개발자
  // const { names, items } = arr.reduce((acc, item) => {
  //   acc.names.push(item.name);
  //   acc.items[item.name] = item;
  //   return acc;
  // }, { names: [], items: {} });

  return (
    <h1>test</h1>
  )
}

export default CheckComponent;